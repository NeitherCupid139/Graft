interface:
  display_name: "Graft PR Review"
  short_description: "Inspect the current PR and AI review findings"
  default_prompt: "Use $graft-pr-review to inspect the current branch PR through the GitHub API, prioritize unresolved review threads on the latest head commit from supported AI reviewers such as CodeRabbit, greptile-apps, and gemini-code-assist, and summarize failed checks or failed tests."
