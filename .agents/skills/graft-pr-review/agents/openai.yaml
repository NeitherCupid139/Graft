interface:
  display_name: "Graft PR Review"
  short_description: "Inspect the current PR and AI review findings"
  default_prompt: "Use $graft-pr-review to inspect the current branch PR with GitHub MCP when available, fall back to the Python inventory helper for structured findings, and classify every AI review finding."
