interface:
  display_name: "Graft Security Remediation"
  short_description: "Triage and close Graft security alerts safely"
  default_prompt: "Use $graft-security-remediation to inventory GitHub Code Scanning and Dependabot alerts, classify true positives vs false positives, remediate the owned scope, validate it, and run the guarded branch/commit/push/PR/recheck flow."
