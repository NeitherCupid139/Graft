interface:
  display_name: "Graft PR Create"
  short_description: "Create or reconcile the current branch PR"
  default_prompt: "Use $graft-pr-create to inspect PR state with GitHub MCP when available, then create or reuse the current branch PR through the guarded helper workflow."
