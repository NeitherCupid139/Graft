# 插件系统与轻量 DI 设计

这份文档是 `server` 的架构与边界设计说明，不再承担日常执行级后端规范的唯一入口。

后端执行级治理真值现在收口到 `server/AGENTS.md`：

- `server/AGENTS.md` 负责日常实现约束，例如 plugin 生命周期、`internal/pluginapi` 与 contract 边界、DI 与 wiring、Go 代码组织、Ent/migration 与 backend validation
- 本文档负责解释为什么采用 plugin-oriented backend、为什么保持轻量 DI，以及为什么这些边界服务于长期可扩展性

如果实现级规则与本文档的表述需要分层，执行时以 `server/AGENTS.md` 为准；若该执行级规则改变了插件系统、DI 职责或后端长期边界，再同步更新本文档。

## 1. 设计目标

这份文档只回答两件事：

- 插件之间如何协作
- 为什么引入轻量 DI，以及它具体负责什么

目标不是引入一个“万能容器”，而是让平台在插件数量增加时仍保持清晰、可测、可维护。

---

## 2. 插件交互模型

插件之间禁止直接依赖彼此内部实现。

统一交互方式：

- 平台核心提供基础设施能力
- 插件通过稳定 capability contract 暴露公开服务
- 其它插件通过接口解析依赖

典型例子：

- `user` 插件公开 `UserService`
- `rbac` 插件依赖 `UserService` 接口，而不是依赖 `plugins/user/service` 的具体 struct
- `user` 只对 `rbac` 暴露稳定用户能力，例如用户存在性检查、基础身份查询、删除前约束检查
- `rbac` 处理 `user_roles` 写入时通过该稳定接口校验 `user_id`，而不是 import `user` 的 Ent 包、repository 或其它私有持久化实现

这样做的收益：

- 插件边界清晰
- 单元测试更容易替换实现
- 后续拆分包结构时影响更小

---

## 3. 轻量 DI 的职责

容器只负责四类事情：

- 注册单例服务
- 解析服务
- 注册插件公开服务
- 统一释放资源

容器不负责：

- 包扫描
- 自动发现 provider
- 通过 tag 注入字段
- 隐式构造依赖图
- 复杂的 request scope / session scope

这意味着代码仍然保持 Go 风格的显式装配，只是把“装配入口”集中管理。

当前长期治理方向补充如下：

- 保持 compile-time modular monolith，而不是演进为 runtime plugin platform
- 允许引入 `plugin.Descriptor`、`plugin.Builder` 与 compile-time generated registry 来减少多工作树共享接线冲突
- 不引入 runtime filesystem scan、runtime plugin discovery、hot-load 或 generalized reflection plugin system
- 启动仍然要求 deterministic wiring 和单体进程语义

---

## 4. 推荐接口

推荐抽象出两个能力：

### 4.1 服务注册

```go
type Registry interface {
    RegisterSingleton(key any, provider Provider) error
    RegisterPluginService(plugin string, key any, provider Provider) error
}
```

### 4.2 服务解析

```go
type Resolver interface {
    Resolve(key any) (any, error)
    MustResolve(key any) any
}
```

如果实现时使用泛型，可以再包装成更易用的 typed API，但内部模型仍应保持“显式注册 + 显式解析”。

---

## 5. 插件上下文建议

插件上下文不应继续无限膨胀。

建议分成三类能力：

### 5.1 基础设施句柄

- 配置
- 日志
- i18n
- 当前过渡期可见的 repository / store factory
- HTTP 路由根
- 事件总线

### 5.2 注册器

- 菜单注册器
- 权限注册器
- 定时任务注册器

### 5.3 服务能力

- Service Registry
- Service Resolver

这样可以避免把所有内容都塞进一个巨大的 `Context` 结构体。

长期并行开发方向补充约束：

- `plugin.Context` 不应无限扩张为“万能业务依赖袋”
- 业务插件私有 repository 依赖更适合在 Builder 阶段装配，而不是长期保留中心化 `store.Factory` 聚合入口
- capability 必须在 Builder 阶段注册，生命周期稳定，不暴露 repository、ORM entity、Ent client、storeent 实现或插件内部 struct
- capability 只允许暴露：
  - cross-plugin business ability
  - dev/reset hook
  - stable query/service contract

当前 MVP 收敛阶段对这些能力补充约束：

- `event bus` 采用 `server/internal/eventbus` 下的最小进程内实现
- `event bus` 生命周期由 `Runtime` 持有并统一注入
- 插件只能依赖其稳定接口，不直接依赖具体实现
- `scheduler` 作为运行期调度能力，应通过独立接口注入，而不是让业务插件直接依赖 `robfig/cron`
- i18n 能力统一经由 `server/internal/i18n` facade 注入；当前阶段先补强自管 registry，而不是在插件面直接绑定未来可能引入的 `go-i18n`

---

## 6. 插件注册阶段约束

`Register` 阶段只做声明式工作：

- 注册 HTTP 路由
- 注册菜单
- 注册权限点
- 注册 message bundles / message keys
- 注册任务定义
- 注册公开服务

`Register` 阶段不要做：

- 长时间初始化
- 启动 goroutine
- 执行耗时任务
- 依赖其它插件已经 Boot 完成的逻辑

`Boot` 阶段才做真正的运行时启动。

补充说明：

- event handler 注册属于 `Register` 阶段的声明式工作
- `scheduler` 读取 `cron registry` 并真正启动任务属于 `Boot` 阶段
- `audit` 的 HTTP middleware 注册属于 `Register` 阶段，event 订阅也应在这一阶段声明完成
- i18n message 注册属于 `Register` 阶段；runtime 必须在进入 `Boot` 前冻结 i18n 注册面，并把 duplicate-key 视为声明期问题而不是运行期兜底
- 插件通过 `plugin.Context.LifecycleContext` 获取当前阶段上下文；`Shutdown` 阶段由 runtime 注入独立的有界关闭 ctx，避免直接复用已取消的 `runCtx`

---

## 7. 服务边界规范

跨插件公开服务建议统一放在 `internal/pluginapi` 或等价稳定位置。

规范如下：

- 暴露接口，不暴露实现细节
- 接口尽量面向业务能力，而不是数据库细节
- 平台共用 typed contract 放 `server/internal/contract`，插件私有稳定契约放 `server/plugins/<plugin>/contract`，
  真正跨插件公开的稳定契约继续放 `server/internal/pluginapi`
- 插件只允许依赖：
  - `server/internal/pluginapi/**`
  - `server/internal/contract/**`
  - 其它插件公开 capability contract 或 stable DTO contract
- 禁止直接依赖其它插件的 `service/**`、`storeent/**`、`ent/schema/**`、migration 目录
- 插件上下文与公开接口不直接暴露 `*ent.Client` 或其它具体 ORM 句柄
- 数据访问通过 repository / store factory 边界下沉到插件内部
- 返回值尽量稳定，不把 Ent entity 直接作为跨插件返回类型
- `user_roles` 这类最终归 `rbac` 拥有的持久化细节，必须留在 `rbac` 自己的 schema / repository / migration / test 边界内，不通过 `user`
  capability 反向泄露实现细节
- `permission code`、`event name`、`message key`、跨插件状态枚举等高风险契约，应遵循
  [契约治理与魔法值治理规范](契约治理与魔法值治理规范.md) 的 canonical ownership、lifecycle 与 compatibility
  规则
- 跨插件注册、发布、解析边界优先消费 typed contract，而不是继续以裸字符串作为长期真值
- 同一语义的跨插件 contract 只允许一个 canonical definition；兼容期 alias 只能用于过渡，不得演变为平行标准
- 当前 locale 范围只收敛到 `zh-CN` / `en-US`；新增插件 message contract 不得假定仓库已进入多语言自由扩展阶段

示例：

- 好：`GetUserByID(ctx, id) (UserSummary, error)`
- 好：`CheckUserExists(ctx, userID) error`
- 好：`CheckUserDeletionConstraints(ctx, userID) error`
- 差：`GetEntClient()`
- 差：`AssignRoleViaUserRepo()`
- 差：`GetUserRepo() *UserRepository`

当前 MVP 收敛阶段新增的边界约束：

- `event bus` 的公开接口只表达 `Subscribe / Publish / handler 注册` 这类最小能力
- 不提前暴露 ack、retry、dead-letter、consumer-group、partition 等分布式 MQ 语义
- `audit` 的跨插件边界优先暴露“写审计记录”或“发送审计事件”的稳定能力，而不是暴露底层仓储
- `scheduler` 的跨插件边界优先暴露 `RegisterJob / RemoveJob / Start / Stop` 这类仓库内稳定接口
- 业务插件禁止直接 import `github.com/robfig/cron/v3`
- 权限声明的稳定元数据（如 `permission code`、`category`）由权限注册声明侧提供 canonical truth，持久化层只负责保存，不在仓储或 migration 默认值里二次推断

---

## 8. 插件失败策略

建议把插件分两类：

- 核心插件：失败时阻止应用启动
- 可选插件：失败时记录错误并跳过

第一阶段默认这些属于核心插件：

- `user`
- `rbac`

这些在更广能力面上仍可保持可选，但在当前 MVP 闭环收敛阶段需要优先补齐实现：

- `audit`
- `scheduler`

---

## 9. 需要测试的关键场景

- 重复注册同一插件
- 缺失依赖插件
- 循环依赖
- 插件公开服务未注册
- 插件请求服务但服务不存在
- 同一服务被重复注册
- `Register -> Boot -> Shutdown` 顺序错误
- 插件关闭时资源未释放
- event handler panic 是否被 recover
- event handler error 是否被稳定记录
- `audit` middleware 与 event 审计路径是否都可用
- `scheduler` 是否能按显式生命周期启动、停止与移除任务

---

## 10. 多工作树与未来兼容边界

为支持长期多工作树并行开发，同时保留未来三方插件生态的可演进边界，当前设计补充以下约束：

- `plugin.Descriptor` 可以作为未来插件元数据基础
- capability interface 是未来跨插件调用边界
- `internal/pluginapi/**` 与 `internal/contract/**` 是未来唯一稳定公开 API 面
- 插件内部实现必须与稳定公开边界严格分离

当前不实现：

- runtime plugin loading
- external plugin marketplace
- hot reload lifecycle
- sandbox execution
- distributed plugin protocol
- microservice decomposition

这样做的目的，是让未来若需要“类似 Jenkins 的三方插件生态”时，仍有稳定的元数据、契约与 capability 边界可复用；
但在当前阶段，仓库仍然只是 compile-time modular monolith。

## 11. 结论

这个项目需要 DI，但只需要“轻量、显式、可调试”的 DI。

判断标准很简单：

- 如果一个新插件接入时，依赖注册与解析路径清晰，那容器设计就是对的
- 如果实现者开始依赖反射魔法、扫描和隐式行为，说明容器已经做过头了
- 如果为了赶 MVP 闭环而把 `event bus`、审计、调度直接泄漏为底层实现细节，说明边界已经被破坏
