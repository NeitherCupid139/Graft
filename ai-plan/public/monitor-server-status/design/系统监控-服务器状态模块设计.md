# 系统监控-服务器状态模块设计

## 1. 设计目标

- 定义 `server` 插件 `monitor` 与 `web` 模块 `monitor` 的统一边界。
- 首个能力固定为 `server-status`，只做一个最小可用的状态查询闭环。
- 保持 `title_key + title fallback`、`index.ts + bootstrap-routes.ts`、`group path + route fragment` 的既有仓库约束。
- 让实现自然落在 `server/plugins/monitor/**` 与 `web/src/modules/monitor/**`。

## 2. 非目标

- 不引入 Prometheus、Grafana、WebSocket 或多节点聚合。
- 不把未来信息架构占位直接接入真实运行菜单、路由或权限。
- 不做告警策略、历史持久化或跨节点拓扑。
- 不把共享热点当成长期 ownership。

## 3. 菜单与信息架构

- 真实运行导航采用一个显式分组段：`服务器管理 / 服务器状态`。
- 后端在当前 slice 中显式注册两个分组级菜单和三个三级叶子菜单：
  - `/monitor`
    - 标题：`服务器管理`
    - 只作为导航父级，不绑定独立运行页面
    - 图标语义：服务器 / 主机
  - `/monitor/server-status`
    - 标题：`服务器状态`
    - 只作为二级信息分组，不直接承载独立页面
    - 图标语义：状态 / 心跳 / 监控
  - `/monitor/server-status/overview`
    - 标题：`概览`
    - 复用当前服务器状态总览页
    - 图标语义：仪表盘 / 总览
  - `/monitor/server-status/runtime`
    - 标题：`运行时`
    - 展示 Go 进程运行时与宿主机环境快照
  - `/monitor/server-status/dependencies`
    - 标题：`依赖服务`
    - 展示 PostgreSQL、Redis 与未来插件依赖健康项入口
- 菜单标题继续使用 `title_key` 作为唯一长期真值，`title` 只作为 fallback。
- 前端按现有 bootstrap 菜单快照装配树形导航，不再依赖硬编码父级标题，也不再让页面或面包屑暴露英文 `index`。
- 当前运行态只落地 `概览 / 运行时 / 依赖服务` 三个三级叶子页。
- 更复杂的扩展方向如 `资源监控`、`健康检查`、`告警记录`、`诊断信息` 只记录在设计文档中，不进入当前运行态。

## 4. 前端模块设计

- 模块目录固定为 `web/src/modules/monitor/**`。
- 模块入口固定为 `index.ts`，动态路由声明固定为 `bootstrap-routes.ts`。
- 模块内部继续按 `pages`、`api`、`contract`、`locales`、`types` 组织。
- 三个真实页面都声明为 `overview-dashboard` 页面类型：
  - `概览`
    - 保留当前状态仪表页
  - `运行时`
    - 展示运行时长、Go 版本、Goroutine 数、Go Runtime 内存、GC 次数、最近 GC 时间和构建元数据空态
  - `依赖服务`
    - 展示 PostgreSQL、Redis 的状态、响应耗时、最近检测时间、错误信息与未来扩展入口
- 页面只负责展示聚合状态和手动刷新，不持有壳层逻辑。
- 路由注册暴露三个 bootstrap 入口，但继续复用同一个后端聚合 API。

## 5. 后端插件设计

- 插件目录固定为 `server/plugins/monitor/**`。
- 插件名固定为 `monitor`，首个能力名固定为 `server-status`。
- 注册阶段负责声明菜单、权限、路由和消息 key。
- 启动阶段只处理已声明的运行时行为，不新增隐式能力。
- 该能力优先聚合现有核心运行时信息，不新增独立持久化层。

## 6. 权限与 RBAC 设计

- 只保留一个读取权限，建议命名为 `monitor.server-status.read`。
- 菜单和 API 都应受同一权限约束。
- MVP 不包含写权限、编辑权限或分级授权。
- 前端只在具备权限时暴露菜单入口。

## 7. API 设计

- 采用单接口聚合。
- 路由契约继续使用 `group path + route fragment`。
- 建议约定：
  - group path: `/monitor`
  - route fragment: `/server-status`
  - endpoint: `GET /api/monitor/server-status`
- 该接口返回页面所需的最小状态汇总。

## 8. 数据结构与响应示例

- 当前返回值固定包含：
  - `status`
  - `observed_at`
  - `server`
  - `runtime`
  - `dependencies`
  - `summary`
  - `trend`
  - `plugins`
- `server` 承载版本、启动时间、运行时长和应用级摘要。
- `runtime` 承载 Go 运行时、宿主机、负载、磁盘与明确区分后的宿主机/Go 进程内存快照。
- `dependencies` 承载数据库、Redis 等核心依赖的状态、说明与延迟样本。
- `summary` 承载依赖和插件的计数型总览，供前端仪表卡片直接消费。
- `trend` 承载 CPU、宿主机内存占用、负载和 Go 进程内存态的短窗口趋势，不做历史持久化。
- `plugins` 承载运行时依赖顺序下的插件版本、依赖关系与状态摘要。

```json
{
  "status": "healthy",
  "observed_at": "2026-05-20T08:00:00Z",
  "server": {
    "version": "dev",
    "started_at": "2026-05-20T07:30:00Z",
    "uptime_seconds": 1800,
    "go_version": "go1.26.0",
    "app_name": "graft",
    "app_env": "prod"
  },
  "runtime": {
    "go_version": "go1.26.0",
    "host_name": "graft-node-01",
    "operating_system": "linux",
    "architecture": "amd64",
    "cpu_cores": 8,
    "load_average": {
      "one_minute": 0.42,
      "five_minutes": 0.31,
      "fifteen_minutes": 0.29
    },
    "disk_usage": {
      "path": "/",
      "total_bytes": 64317135257,
      "used_bytes": 12670153523,
      "free_bytes": 51646981734,
      "used_percent": 19.8
    },
    "host_memory_total_bytes": 34359738368,
    "host_memory_used_bytes": 16234976358,
    "host_memory_free_bytes": 18124762010,
    "host_memory_used_percent": 47.25,
    "goroutines": 42,
    "runtime_alloc_bytes": 62914560,
    "runtime_heap_in_use_bytes": 50331648,
    "runtime_sys_bytes": 117440512,
    "runtime_gc_cycles": 9
  },
  "dependencies": {
    "database": {
      "status": "healthy",
      "detail": "Database ping succeeded",
      "latency_ms": 2.15
    },
    "redis": {
      "status": "disabled",
      "detail": "Redis client is not configured",
      "latency_ms": null
    }
  },
  "summary": {
    "total_dependencies": 2,
    "healthy_dependencies": 1,
    "degraded_dependencies": 0,
    "unknown_dependencies": 0,
    "disabled_dependencies": 1,
    "total_plugins": 4,
    "healthy_plugins": 0
  },
  "trend": {
    "range": "10m",
    "retention_seconds": 600,
    "sample_interval_seconds": 5,
    "points": [
      {
        "observed_at": "2026-05-20T07:59:30Z",
        "cpu_percent": 14.2,
        "host_memory_used_percent": 46.71,
        "load_average_one_minute": 0.39,
        "load_average_five_minutes": 0.32,
        "load_average_fifteen_minutes": 0.28,
        "goroutines": 40,
        "runtime_alloc_bytes": 60817408,
        "runtime_heap_in_use_bytes": 48234496,
        "runtime_sys_bytes": 115343360
      },
      {
        "observed_at": "2026-05-20T08:00:00Z",
        "cpu_percent": 18.6,
        "host_memory_used_percent": 47.25,
        "load_average_one_minute": 0.42,
        "load_average_five_minutes": 0.31,
        "load_average_fifteen_minutes": 0.29,
        "goroutines": 42,
        "runtime_alloc_bytes": 62914560,
        "runtime_heap_in_use_bytes": 50331648,
        "runtime_sys_bytes": 117440512
      }
    ]
  },
  "plugins": [
    {
      "name": "monitor",
      "status": "healthy",
      "status_detail": "Runtime metadata is present and platform signals are healthy",
      "version": "0.1.0",
      "depends_on": ["user", "rbac"]
    }
  ]
}
```

- 字段语义约束：
  - `runtime.host_memory_*` 表示宿主机内存快照，供前端顶部内存总览卡直接消费。
  - `runtime.runtime_*` 表示当前 Go 进程 / runtime 指标，不与宿主机内存混用。
  - `trend.points[*].host_memory_used_percent` 表示宿主机内存使用率趋势。
  - `trend.points[*].load_average_*` 表示与 `runtime.load_average` 同语义的采样点序列。
  - `trend.points[*].runtime_*` 表示 Go 进程 / runtime 的趋势字段。

- `trend.points` 当前只在插件进程内保留短窗口数据：
  - retention 固定为 10 分钟
  - sample interval 固定为 5 秒
  - 不做跨重启恢复
  - 手动高频刷新会复写同一采样窗口末端点，而不是伪造更高采样频率

## 9. i18n 设计

- `title_key` 继续作为菜单和动态路由标题的唯一长期真值。
- `title` 只在装配边界用于外部输入回退。
- 前端模块消息建议使用 `monitor.*` 前缀。
- 当前运行态使用两个稳定 key：
  - `monitor.sectionTitle`
  - `menu.monitor.server_status.title`
- 后端错误和状态消息保持插件内收口，不外泄为裸文案。

## 10. MVP 范围

- 一个插件：`monitor`。
- 一个前端模块：`monitor`。
- 一个能力：`server-status`。
- 一个真实菜单父级：`服务器管理`，仅用于导航层级和面包屑语义。
- 一个二级分组：`服务器状态`，负责承接后续三级 IA 扩展。
- 一个 API：聚合状态查询。
- 三个真实运行页：
  - `概览`
    - 带负载、CPU、内存、磁盘四张首屏监控卡、依赖面板、运行时信息和短窗口趋势图的状态仪表页
  - `运行时`
    - 聚焦 Go 进程运行时、GC、构建元数据和宿主机环境
  - `依赖服务`
    - 聚焦 PostgreSQL、Redis 与未来插件依赖健康项入口

## 11. 后续扩展方向

- 信息架构预留：
  - `资源监控`
  - `健康检查`
  - `告警记录`
  - `诊断信息`
- 增加更细粒度的依赖状态。
- 增加趋势视图或历史快照。
- 增加告警能力。
- 在真正需要时再评估多节点与实时推送。

## 12. 风险与取舍

- 单接口聚合容易变胖，必须控制返回字段数量。
- 只做读模型意味着它不是完整可观测平台。
- 趋势窗口只保留进程内短期数据，页面刷新或进程重启后不保证连续历史。
- 当前状态页继续采用轮询而不是 WebSocket：
  - 页面只读且默认 5 秒轮询已足够覆盖当前单页总览需求
  - 页面不可见暂停和失败退避可以避免无意义高频请求
  - 真正需要准实时多流推送时再重新评估 SSE 或 WebSocket
- 不引入外部监控栈能保持边界清晰，但也意味着指标深度有限。
- 若后续扩展跨到共享热点，必须重新切片，而不是顺手扩写。
- 前端图表与仪表卡片必须依赖主题 token 和颜色模式响应；不允许硬编码只在单一明暗模式下可用的颜色。
- 当前页面首屏四卡固定对应负载、CPU、内存和磁盘，插件运行详情暂时隐藏。

## 13. 验收标准

- 插件名为 `monitor`，模块名为 `monitor`。
- 首个能力名为 `server-status`。
- 采用 `server/plugins/monitor/**` 与 `web/src/modules/monitor/**`。
- 真实运行导航表现为 `服务器管理 / 服务器状态 / 概览|运行时|依赖服务`，且面包屑不得出现英文 `index`。
- 当前运行态只存在 `概览 / 运行时 / 依赖服务` 三个三级页面；更复杂 IA 占位不进入运行菜单、路由或权限。
- 继续使用 `title_key + title fallback`。
- 前端继续使用 `index.ts + bootstrap-routes.ts`。
- 后端路由继续使用 `group path + route fragment`。
- API 返回 `server + runtime + dependencies + summary + trend + plugins` 的 richer payload。
- 仪表页在主题切换和 brand theme 切换下仍保持图表、卡片、边框和文案可读性。
- MVP 不包含 Prometheus、Grafana、WebSocket 或多节点。
