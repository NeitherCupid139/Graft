// Package rbac 提供 MVP 阶段最小可用的后端授权与管理模块。
//
// 当前实现把基于仓储的权限判断能力暴露为稳定的 `moduleapi.Authorizer`，
// 同时提供角色/权限只读接口，以及最小写接口：角色创建、角色更新、
// 角色权限替换和用户角色替换。更完整的删除、禁用或特权旁路治理仍保留在
// 后续 RBAC 管理切片中收敛。
package rbac
