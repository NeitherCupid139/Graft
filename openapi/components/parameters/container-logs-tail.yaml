name: tail
in: query
required: false
description: Number of log lines to return from the end of the stream.
schema:
  type: integer
  minimum: 1
  maximum: 2000
  default: 200
examples:
  defaultTail:
    summary: Default tail window
    value: 200
  maxTail:
    summary: Maximum accepted tail window
    value: 2000
