name: ticket
in: query
required: true
description: >
  Opaque single-use realtime subscription ticket issued by the authenticated realtime subscription endpoint. The
  server must validate and consume this ticket before upgrading the connection to WebSocket.
schema:
  type: string
  minLength: 1
  maxLength: 2048
