type: object
required:
  - items
  - total
  - page
  - page_size
properties:
  items:
    type: array
    items:
      $ref: ./audit-log-list-item.yaml
  total:
    type: integer
  page:
    type: integer
  page_size:
    type: integer
  applied_scope:
    $ref: ./applied-drilldown-scope.yaml
  scope_projection:
    $ref: ./drilldown-scope-projection.yaml
  convertible_filters:
    $ref: ./audit-log-convertible-filters.yaml
