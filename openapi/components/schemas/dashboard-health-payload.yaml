type: object
required: [summary, items]
properties:
  summary:
    type: object
    required: [status]
    properties:
      status:
        $ref: ./dashboard-health-status.yaml
      label_key:
        type: string
      label:
        type: string
  items:
    type: array
    items:
      type: object
      required: [key, label_key, label, status]
      properties:
        key:
          type: string
        label_key:
          type: string
        label:
          type: string
        status:
          $ref: ./dashboard-health-status.yaml
        description_key:
          type: string
        description:
          type: string
        route_location:
          type: string
  healthy_modules:
    type: integer
    minimum: 0
    description: Optional count of healthy modules for module-runtime health summaries.
    example: 7
  abnormal_services:
    type: integer
    minimum: 0
    description: Optional count of module or service entries that need attention.
    example: 0
