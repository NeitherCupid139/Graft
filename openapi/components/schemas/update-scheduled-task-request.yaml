type: object
properties:
  title:
    type: string
    minLength: 1
  description:
    type: string
  cron_expression:
    type: string
    minLength: 1
  enabled:
    type: boolean
  params_json:
    type: string
    description: JSON parameters passed to the Job Definition handler when this Scheduled Task runs.
